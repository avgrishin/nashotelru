@model dynamic
  @{
    ViewBag.Title = Nashotelru.Resources.Shared.Menu.DM_Vacances;
  }
  <div class="row">
    <div class="col-md-4 col-lg-2"></div>
    <div class="col-md-8 col-lg-8 no-h1">
      <h2>@ViewBag.Title</h2>
      @Html.Raw(Model.Page == null ? "" : Model.Page.Content)
    </div>
  </div>

